const Ships = require("./Ships");

describe("ships processes", () => {
  beforeEach(() => {
    //initialize creation of all ship type
  });
  test("", () => {
    //test rightness of ships creation
  });
});
